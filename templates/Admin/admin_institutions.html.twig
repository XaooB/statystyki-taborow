{% extends 'base.html.twig' %}

{% block body %}
    {% include 'Admin/toolbar.html.twig' %}
    
    <div class="admin__header">
        <h1>Instytucje</h1>
        <a class="button" href="{{ path('admin_institutions_new') }}">{{ getText('admin_add_new') }}</a>
    </div>
    <table>
        <tr>
            <th>Nazwa</th>
            <th>Slug</th>
            <th>Akcje</th>
        </tr>
        {% for institution in institutions %}
            <tr>
                <td>{{ institution.name }}</td>
                <td>{{ institution.slug }}</td>
                <td>
                    <button class="button">{{ getText('admin_delete') }}</button>
                    <button class="button">{{ getText('admin_edit') }}</button>
                </td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}